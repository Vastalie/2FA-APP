<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
</head>
<body>
    <h2>Welcome, <%= user.username %>!</h2>

    <h3>Your Account Activity</h3>
    <p><strong>Registered At:</strong> <%= user.registered_at %></p>
    <p><strong>Last Login:</strong> <%= user.last_login ? user.last_login : "Not recorded" %></p>
    <p><strong>Last 2FA Verification:</strong> <%= user.last_2fa ? user.last_2fa : "Not recorded" %></p>
    <p><strong>Device Used:</strong> <%= user.last_device ? user.last_device : "Unknown" %></p>

    <a href="/logout">
        <button>Logout</button>
    </a>
</body>
</html>
